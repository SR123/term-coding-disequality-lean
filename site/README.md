# Interactive paper website

See [the companion guide](../README.md) for the mathematical layers, source provenance, verification and later GitHub Pages publication.

Run `npm ci`, then `npm run dev` for a local editing preview. `npm run build` produces the standalone static files in `dist/client/`.
