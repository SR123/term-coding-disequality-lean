{"dependencies":["Not","Decidable","Decidable.casesOn"],"doc":"\"Dependent\" if-then-else, normally written via the notation `if h : c then t(h) else e(h)`,\nis sugar for `dite c (fun h => t(h)) (fun h => e(h))`, and it is the same as\n`if c then t else e` except that `t` is allowed to depend on a proof `h : c`,\nand `e` can depend on `h : ¬c`. (Both branches use the same name for the hypothesis,\neven though it has different types in the two cases.)\n\nWe use this to be able to communicate the if-then-else condition to the branches.\nFor example, `Array.get arr i h` expects a proof `h : i < arr.size` in order to\navoid a bounds check, so you can write `if h : i < arr.size then arr.get i h else ...`\nto avoid the bounds check inside the if branch. (Of course in this case we have only\nlifted the check into an explicit `if`, but we could also use this proof multiple times\nor derive `i < arr.size` from some other proposition that we are checking in the `if`.)\n","extra":null,"kind":"definition","levels":["u"],"module":"Init.Prelude","name":"dite","nodes":[["sort",["param","u"]],["sort",["zero"]],["constant","Decidable",[]],["bound",0],["apply",2,3],["bound",1],["bound",3],["forall","a._@._internal._hyg.0",5,6,"explicit"],["constant","Not",[]],["bound",2],["apply",8,9],["bound",4],["forall","a._@._internal._hyg.0",10,11,"explicit"],["forall","e",12,11,"explicit"],["forall","t",7,13,"explicit"],["forall","h",4,14,"instance"],["forall","c",1,15,"explicit"],["forall","α",0,16,"implicit"],["constant","Decidable.casesOn",[["param","u"]]],["apply",18,6],["apply",2,6],["bound",5],["lambda","x._@.Init.Prelude.3893281826._hygCtx._hyg.15",20,21,"explicit"],["apply",19,22],["apply",23,9],["apply",24,3],["apply",25,5],["lambda","e",12,26,"explicit"],["lambda","t",7,27,"explicit"],["lambda","h",4,28,"instance"],["lambda","c",1,29,"explicit"],["lambda","α",0,30,"implicit"]],"project":false,"range":{"column":0,"endColumn":15,"endLine":1055,"line":1040},"type":17,"typeText":"{α : Sort u} → (c : Prop) → [h : Decidable c] → (c → α) → (Not c → α) → α","unsafe":false,"value":31,"source":{"code":"/--\n\"Dependent\" if-then-else, normally written via the notation `if h : c then t(h) else e(h)`,\nis sugar for `dite c (fun h => t(h)) (fun h => e(h))`, and it is the same as\n`if c then t else e` except that `t` is allowed to depend on a proof `h : c`,\nand `e` can depend on `h : ¬c`. (Both branches use the same name for the hypothesis,\neven though it has different types in the two cases.)\n\nWe use this to be able to communicate the if-then-else condition to the branches.\nFor example, `Array.get arr i h` expects a proof `h : i < arr.size` in order to\navoid a bounds check, so you can write `if h : i < arr.size then arr.get i h else ...`\nto avoid the bounds check inside the if branch. (Of course in this case we have only\nlifted the check into an explicit `if`, but we could also use this proof multiple times\nor derive `i < arr.size` from some other proposition that we are checking in the `if`.)\n-/\n@[macro_inline] def dite {α : Sort u} (c : Prop) [h : Decidable c] (t : c → α) (e : Not c → α) : α :=\n  h.casesOn e t","start":1040,"end":1055},"axioms":[]}